# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: gitflow-analytics
  version: 3.14.23

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/gitflow_analytics-${{ version }}.tar.gz
  sha256: 7c66e0de32a73f1f50e00895d86a0dfe130c36382e5ec8a1edbbae19fe02dc25

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - gitflow-analytics = gitflow_analytics.cli:main
      - gfa = gitflow_analytics.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61
    - wheel
    - pip
  run:
    - python >=${{ python_min }},<4.0
    - click >=8.1
    - gitpython >=3.1
    - pygithub >=2.0
    - tqdm >=4.65
    - sqlalchemy >=2.0
    - pandas >=2.0
    - pyyaml >=6.0
    - python-dateutil >=2.8
    - python-dotenv >=1.0
    - rich >=13.0.0
    - tabulate >=0.9.0
    - numpy >=1.24.0
    - joblib >=1.5.3
    - pytz >=2024.1
    - requests >=2.28.0
    - scipy
    - httpx
    - urllib3
    - toml
tests:
  - python:
      imports:
        - gitflow_analytics
      pip_check: true
      python_version:
        - ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - gitflow-analytics --help
      - gfa --help

about:
  summary: Analyze Git repositories for developer productivity insights
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/bobmatnyc/gitflow-analytics

extra:
  recipe-maintainers:
    - rxm7706
